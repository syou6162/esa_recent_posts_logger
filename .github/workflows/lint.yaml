name: lint

on: [push]

jobs:
  title_validation:
    runs-on: ubuntu-16.04
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - uses: actions/setup-node@v2
      with:
        node-version: '12'
    - run: npm install
    - name: lint
      run: npx eslint --ext .ts src
